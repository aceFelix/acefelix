/**
 * 3D 知识图谱视觉参数配置
 * 修改后保存即可生效（Vite 热更新），无需重启服务
 */

export const graphConfig = {
  // ---------- 节点外观 ----------
  node: {
    /** 无连接节点的基础半径 */
    baseRadius: 9.0,
    /** 每多一条连接，半径增加量 */
    radiusPerDegree: 0.7,
    /** 视觉半径上限（防止中心大节点吞噬画面） */
    maxRadius: 15.0,
    /** 力导向布局中，每条连接增加的"质量"（影响力学，不影响视觉大小） */
    valPerDegree: 0.25,
    /** 布局质量上限 */
    maxVal: 4,
  },

  // ---------- 力导向布局 ----------
  force: {
    /** 节点间斥力（绝对值越大越分散） */
    chargeStrength: -600,
    /** 连线理想长度 */
    linkDistance: 100,
    /** 碰撞检测时，在球体半径外额外保留的边距（防标签贴脸） */
    collidePadding: 3,
    /** 布局演算轮数（越多越充分，但首屏等待越长） */
    cooldownTicks: 400,
    /** alpha 衰减速度（越小布局收敛越慢、越充分） */
    alphaDecay: 0.01,
  },

  // ---------- 相机 ----------
  camera: {
    /** 初始相机距离（z 轴） */
    initialZ: 320,
    /** 自动适配全图时的边距倍数（1 = 刚好贴边，2.4 = 宽松） */
    fitMargin: 2.4,
    /** 自动适配的最小距离（防止空图时怼脸） */
    minFitDistance: 160,
    /** 聚焦单个节点时的相机距离 */
    focusDistance: 120,
  },
}
